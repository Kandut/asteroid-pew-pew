import * as shop from "./util/shop.js";

export const modifiers = {
    bullet_attack_speed: 1, // base speed * mod
    bullet_compression: 1, // damage * mod ; bullet attack speed / mod
    rocket_attack_speed: 1, // base speed * mod
    rocket_piercing: 0, // base piercing + mod
    experience_gain: 1, // base gains * mod
}